interface Props {
	searchParams: {
		search: string
		page: number
		limit: number
	}
}

const CollaborationsPage = async ({ searchParams }: Props) => {
	return null
}

export default CollaborationsPage
